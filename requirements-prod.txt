# Production Dependencies for Cloud Run
# Excludes large ML libraries (torch, diffusers, transformers, Pillow) since image generation is disabled
# Use this for Cloud Run deployment via Docker

# Firebase and Google Cloud
google-auth>=2.23.0
google-cloud-firestore>=2.13.0

# HTTP and API
requests>=2.31.0
httpx>=0.25.0

# Web Framework
fastapi>=0.104.1
uvicorn[standard]>=0.24.0

# Data Validation
pydantic>=2.5.0
pydantic-settings>=2.0.0

# Environment Management
python-dotenv>=1.0.0

# Brain memory system
sentence-transformers>=2.2.0
numpy>=1.24.0
